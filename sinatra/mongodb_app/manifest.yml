---
applications:
  .:
    name: jruby_mongodb_app
    url: ${name}.${target-base}
    framework:
      name: java_web
      info:
        mem: 512M
        description: Java Web Application
        exec:
    mem: 512M
    instances: 1
    services:
      mongodb1:
        type: :mongodb
...
